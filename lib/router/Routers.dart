import 'package:bank_clean_flutter/pages/bank/backAppraiseDetail.dart';
import 'package:bank_clean_flutter/pages/bank/backCleanDemand.dart';
import 'package:bank_clean_flutter/pages/bank/backSchedulingApply.dart';
import 'package:bank_clean_flutter/pages/bank/bankEvaluation.dart';
import 'package:bank_clean_flutter/pages/bank/bankHome.dart';
import 'package:bank_clean_flutter/pages/bank/bankOutletsSelect.dart';
import 'package:bank_clean_flutter/pages/bank/bankSchedulingDemandList.dart';
import 'package:bank_clean_flutter/pages/check/checkAddScheduling.dart';
import 'package:bank_clean_flutter/pages/check/checkAssessmentDetail.dart';
import 'package:bank_clean_flutter/pages/check/checkAssessmentScored.dart';
import 'package:bank_clean_flutter/pages/check/checkAssessmentScoring.dart';
import 'package:bank_clean_flutter/pages/check/checkCleanDemand.dart';
import 'package:bank_clean_flutter/pages/check/checkCleanMonthlyAssessment.dart';
import 'package:bank_clean_flutter/pages/check/checkComputationList.dart';
import 'package:bank_clean_flutter/pages/check/checkEmployeeList.dart';
import 'package:bank_clean_flutter/pages/check/checkEventDetail.dart';
import 'package:bank_clean_flutter/pages/check/checkEventReport.dart';
import 'package:bank_clean_flutter/pages/check/checkEventReportList.dart';
import 'package:bank_clean_flutter/pages/check/checkFeedbackDetail.dart';
import 'package:bank_clean_flutter/pages/check/checkFeedbackList.dart';
import 'package:bank_clean_flutter/pages/check/checkHome.dart';
import 'package:bank_clean_flutter/pages/check/checkMachineDetail.dart';
import 'package:bank_clean_flutter/pages/check/checkMachineInspect.dart';
import 'package:bank_clean_flutter/pages/check/checkMachineList.dart';
import 'package:bank_clean_flutter/pages/check/checkMaterielExamine.dart';
import 'package:bank_clean_flutter/pages/check/checkNoticeDetail.dart';
import 'package:bank_clean_flutter/pages/check/checkScheduling.dart';
import 'package:bank_clean_flutter/pages/check/checkSchedulingEdit.dart';
import 'package:bank_clean_flutter/pages/check/checkWorkAttendance.dart';
import 'package:bank_clean_flutter/pages/check/checkleaveApplyDetail.dart';
import 'package:bank_clean_flutter/pages/check/employeeEntry.dart';
import 'package:bank_clean_flutter/pages/check/examineNoticeDetail.dart';
import 'package:bank_clean_flutter/pages/common/cleanSelect.dart';
import 'package:bank_clean_flutter/pages/common/cleanerWork.dart';
import 'package:bank_clean_flutter/pages/common/dutyPublicSelect.dart';
import 'package:bank_clean_flutter/pages/common/floorSelect.dart';
import 'package:bank_clean_flutter/pages/common/message/messageDetail.dart';
import 'package:bank_clean_flutter/pages/common/occupationSelect.dart';
import 'package:bank_clean_flutter/pages/common/outletsSelect.dart';
import 'package:bank_clean_flutter/pages/common/repair/deviceInfo.dart';
import 'package:bank_clean_flutter/pages/common/repair/equipmentRepair.dart';
import 'package:bank_clean_flutter/pages/common/repair/repairDetail.dart';
import 'package:bank_clean_flutter/pages/common/repair/repairList.dart';
import 'package:bank_clean_flutter/pages/common/repair/reportRepair.dart';
import 'package:bank_clean_flutter/pages/projectManager/assetInventory.dart';
import 'package:bank_clean_flutter/pages/projectManager/equipmentScrapDetail.dart';
import 'package:bank_clean_flutter/pages/projectManager/equipmentScrapList.dart';
import 'package:bank_clean_flutter/pages/projectManager/inventoryDetail.dart';
import 'package:bank_clean_flutter/pages/projectManager/inventoryRecordList.dart';
import 'package:bank_clean_flutter/pages/regionalManager/goToScheduling.dart';
import 'package:bank_clean_flutter/pages/check/materielComputationDetail.dart';
import 'package:bank_clean_flutter/pages/check/materielExamineDetail.dart';
import 'package:bank_clean_flutter/pages/check/onlineInspectionAdd.dart';
import 'package:bank_clean_flutter/pages/check/onlineInspectionDetail.dart';
import 'package:bank_clean_flutter/pages/check/onlineInspectionList.dart';
import 'package:bank_clean_flutter/pages/check/onlineInspectionRecord.dart';
import 'package:bank_clean_flutter/pages/regionalManager/leaveApprovalList.dart';
import 'package:bank_clean_flutter/pages/regionalManager/regionMaterialArea.dart';
import 'package:bank_clean_flutter/pages/regionalManager/schedulingDemandList.dart';
import 'package:bank_clean_flutter/pages/clean/cleanFeedback.dart';
import 'package:bank_clean_flutter/pages/clean/cleanHome.dart';
import 'package:bank_clean_flutter/pages/clean/cleanWorkRecord.dart';
import 'package:bank_clean_flutter/pages/clean/eventDetail.dart';
import 'package:bank_clean_flutter/pages/clean/eventList.dart';
import 'package:bank_clean_flutter/pages/clean/eventReport.dart';
import 'package:bank_clean_flutter/pages/clean/feedbackDetail.dart';
import 'package:bank_clean_flutter/pages/clean/leaveApply.dart';
import 'package:bank_clean_flutter/pages/clean/leaveApplyList.dart';
import 'package:bank_clean_flutter/pages/clean/leaveApplyDetail.dart';
import 'package:bank_clean_flutter/pages/clean/materielApplyList.dart';
import 'package:bank_clean_flutter/pages/clean/materielApply.dart';
import 'package:bank_clean_flutter/pages/clean/materielDetail.dart';
import 'package:bank_clean_flutter/pages/clean/materielSelect.dart';
import 'package:bank_clean_flutter/pages/clean/workArrange.dart';
import 'package:bank_clean_flutter/pages/common/Index/Index.dart';
import 'package:bank_clean_flutter/pages/common/login/Login.dart';
import 'package:bank_clean_flutter/pages/common/message/messageList.dart';
import 'package:bank_clean_flutter/pages/common/splash/Splash.dart';
import 'package:bank_clean_flutter/pages/projectManager/projectAppraiseDetail.dart';
import 'package:bank_clean_flutter/pages/projectManager/projectCheckAssessmentDetail.dart';
import 'package:bank_clean_flutter/pages/projectManager/projectCheckSelect.dart';
import 'package:bank_clean_flutter/pages/projectManager/projectCustomerOpinionDetail.dart';
import 'package:bank_clean_flutter/pages/projectManager/projectCustomerOpinionList.dart';
import 'package:bank_clean_flutter/pages/projectManager/projectHome.dart';
import 'package:bank_clean_flutter/pages/projectManager/projectMaterielComputationDetail.dart';
import 'package:bank_clean_flutter/pages/projectManager/projectMaterielManageDetail.dart';
import 'package:bank_clean_flutter/pages/projectManager/projectMaterielManageList.dart';
import 'package:bank_clean_flutter/pages/projectManager/projectOutletsCheckList.dart';
import 'package:bank_clean_flutter/pages/projectManager/projectOutletsDetail.dart';
import 'package:bank_clean_flutter/pages/projectManager/projectOutletsDistribute.dart';
import 'package:bank_clean_flutter/pages/projectManager/projectOutletsManageDetail.dart';
import 'package:bank_clean_flutter/pages/projectManager/projectOutletsManageList.dart';
import 'package:bank_clean_flutter/pages/projectManager/projectPersonnelAssessmentList.dart';
import 'package:bank_clean_flutter/pages/regionalManager/bankFeedback.dart';
import 'package:bank_clean_flutter/pages/regionalManager/regionHome.dart';
import 'package:bank_clean_flutter/pages/repairman/repairHome.dart';
import 'package:fluro/fluro.dart';
import 'package:flutter/material.dart';

///路由表
class Routers {
  static String root = "/";
  static String loginPage = "/login";
  static String indexPage = "/index";
  static String splashPage = "/splash";

  static String messageList = "/messageList";

  static String messageDetail = "/messageDetail";

  // 保洁选择
  static String cleanSelect = "/common/cleanSelect";

  // 网点选择
  static String outletsSelect = "/common/outletsSelect";

  // 楼层选择
  static String floorSelect = "/common/floorSelect";

  // 保洁工作
  static String cleanerWork = "/common/cleanerWork";

  // 职业选择列表 不公用 巡检+区域经理 --> 排班添加修改
  static String dutyPublicSelect = "/common/dutyPublicSelect";

  // 职业选择列表 不公用
  static String occupationSelect = "/common/occupationSelect";

  // 报修记录列表
  static String repairList = "/common/repairList";

  // 报修记录详情
  static String repairDetail = "/common/repairDetail";

  // 设备信息
  static String deviceInfo = "/common/DeviceInfo";

  // 设备报修
  static String equipmentRepair = "/common/equipmentRepair";

  // 报修处理
  static String reportRepair = "/common/reportRepair";

  /* ---------------------------------保洁 ------------------------------------------ */
  // 保洁首页
  static String cleanHome = "/clean/cleanHome";

  // 清扫反馈
  static String cleanFeedback = "/clean/cleanFeedback";

  // 清扫反馈详情
  static String feedbackDetail = "/clean/feedbackDetail";

  // 物料申请列表
  static String materielApplyList = "/clean/materielApplyList";

  // 物料申请
  static String materielApply = "/clean/materielApply";

  // 物料申请选择
  static String materielSelect = "/clean/materielSelect";

  // 物料申请选择
  static String materielDetail = "/clean/materielDetail";

  // 请假申请列表
  static String leaveApplyList = "/clean/leaveApplyList";

  // 请假申请
  static String leaveApply = "/clean/leaveApply";

  // 工作安排
  static String workArrange = "/clean/WorkArrange";

  // 请假申请详情
  static String leaveApplyDetail = "/clean/leaveApplyDetail";

  // 事件列表
  static String eventList = "/clean/eventList";

  // 事件详情
  static String eventDetail = "/clean/eventDetail";

  // 事件上报
  static String eventReport = "/clean/eventReport";

  // 考勤记录
  static String cleanWorkRecord = "/clean/cleanWorkRecord";

  /* -------------------------------- 领班 ------------------------------------------ */
  // 巡检首页
  static String checkHome = "/check/checkHome";

  // 巡检排班
  static String checkScheduling = "/check/checkScheduling";

  // 清扫需求发布
  static String checkCleanDemand = "/check/checkCleanDemand";

  // 物料审批
  static String checkMaterielExamine = "/check/checkMaterielExamine";

  // 物料审批详情
  static String materielExamineDetail = "/check/materielExamineDetail";

  // 物料核算列表
  static String checkComputationList = "/check/checkComputationList";

  // 物料单详情
  static String materielComputationDetail = "/check/materielComputationDetail";

  // 员工录入
  static String employeeEntry = "/check/employeeEntry";

  // 请假审批列表
  static String checkLeaveApplyDetail = "/check/checkLeaveApplyDetail";

  // 审核通知详情
  static String examineNoticeDetail = "/check/examineNoticeDetail";

  // 巡检通知详情
  static String checkNoticeDetail = "/check/checkNoticeDetail";

  // 巡检考勤
  static String checkWorkAttendance = "/check/checkWorkAttendance";

  // 新增排班
  static String checkAddScheduling = "/check/checkAddScheduling";

  // 排班调整
  static String checkSchedulingEdit = "/check/checkSchedulingEdit";

  // 事件上报列表
  static String checkEventReportList = "/check/checkEventReportList";

  // 巡检事件上报详情
  static String checkEventDetail = "/check/checkEventDetail";

  // 巡检事件处理
  static String checkEventReport = "/check/checkEventReport";

  // 巡检事件反馈
  static String checkFeedbackList = "/check/checkFeedbackList";

  // 巡检清扫反馈详情
  static String checkFeedbackDetail = "/check/checkFeedbackDetail";

  // 巡检保洁月考核
  static String checkCleanMonthlyAssessment =
      "/check/checkCleanMonthlyAssessment";

  // 巡检保洁考核详情
  static String checkAssessmentDetail = "/check/checkAssessmentDetail";

  // 巡检保洁考核打分
  static String checkAssessmentScoring = "/check/checkAssessmentScoring";

  // 巡检保洁考核已打分
  static String checkAssessmentScored = "/check/checkAssessmentScored";

  // 巡检员工名单列表
  static String checkEmployeeList = "/check/checkEmployeeList";

  // 巡检网点巡检列表
  static String onlineInspectionList = "/check/onlineInspectionList";

  // 巡检网点巡检记录
  static String onlineInspectionRecord = "/check/onlineInspectionRecord";

  // 巡检网点巡检详情
  static String onlineInspectionDetail = "/check/onlineInspectionDetail";

  // 巡检新增网点巡检记录
  static String onlineInspectionAdd = "/check/onlineInspectionAdd";

  // 机器保养检查记录
  static String checkMachineList = "/check/checkMachineList";

  // 机器保养检查详情
  static String checkMachineDetail = "/check/checkMachineDetail";

  // 保养检查
  static String checkMachineInspect = "/check/checkMachineInspect";

  /* -------------------------------- 项目主管 -------------------------------------- */
  // 区域经理首页
  static String regionHome = "/regionalManager/regionHome";

  // 排班需求列表
  static String schedulingDemandList = "/regionalManager/schedulingDemandList";

  // 去排班
  static String goToScheduling = "/regionalManager/goToScheduling";

  // 请假审批列表
  static String leaveApprovalList = "/check/leaveApprovalList";

  // 资产盘点记录列表
  static String inventoryRecordList = "/regionalManager/inventoryRecordList";

  // 资产盘点记录详情
  static String inventoryDetail = "/regionalManager/inventoryDetail";

  // 物料区域
  static String regionMaterialArea = "/regionalManager/RegionMaterialArea";

  /* -------------------------------- 银行 ------------------------------------------ */
  // 银行首页
  static String bankHome = "/bank/bankHome";

  // 银行考评
  static String bankEvaluation = "/bank/bankEvaluation";

  // 银行清扫需求发布
  static String backCleanDemand = "/bank/backCleanDemand";

  // 银行排班申请
  static String backSchedulingApply = "/bank/backSchedulingApply";

  // 银行网点切换
  static String bankOutletsSelect = "/bank/bankOutletsSelect";

  // 银行考评详情
  static String backAppraiseDetail = "/bank/backAppraiseDetail";

  // 银行意见反馈
  static String bankFeedback = "/bank/bankFeedback";

  // 银行排班申请记录列表
  static String bankSchedulingDemandList = "/bank/bankSchedulingDemandList";

  /* ------------------------------ 区域经理 ------------------------------------------*/

  // 经理首页
  static String projectHome = "/projectManager/projectHome";

  // 经理网点分配列表
  static String projectOutletsDistribute =
      "/projectManager/projectOutletsDistribute";

  // 经理网点信息详情
  static String projectOutletsDetail = "/projectManager/projectOutletsDetail";

  // 经理巡检选择
  static String projectCheckSelect = "/projectManager/projectCheckSelect";

  // 经理网点管理列表
  static String projectOutletsManageList =
      "/projectManager/projectOutletsManageList";

  // 经理网点管理详情
  static String projectOutletsManageDetail =
      "/projectManager/projectOutletsManageDetail";

  // 经理考评详情
  static String projectAppraiseDetail = "/projectManager/projectAppraiseDetail";

  // 经理物料管理列表
  static String projectMaterielManageList =
      "/projectManager/projectMaterielManageList";

  // 经理物料管理详情
  static String projectMaterielManageDetail =
      "/projectManager/projectMaterielManageDetail";

  // 经理人员考核列表
  static String projectPersonnelAssessmentList =
      "/projectManager/projectPersonnelAssessmentList";

  // 经理巡检考核详情
  static String projectCheckAssessmentDetail =
      "/projectManager/projectCheckAssessmentDetail";

  // 经理网点巡检
  static String projectOutletsCheckList =
      "/projectManager/ProjectOutletsCheckList";

  // 经理客户意见列表
  static String projectCustomerOpinionList =
      "/projectManager/projectCustomerOpinionList";

  // 经理客户意见详情
  static String projectCustomerOpinionDetail =
      "/projectManager/projectCustomerOpinionDetail";

  // 经理物料单详情
  static String projectMaterielComputationDetail =
      "/projectManager/projectMaterielComputationDetail";

  // 设备报废列表
  static String equipmentScrapList = "/projectManager/equipmentScrapList";

  // 设备报废详情
  static String equipmentScrapDetail = "/projectManager/equipmentScrapDetail";

  // 资产盘点
  static String assetInventory = "/projectManager/assetInventory";

  /* ------------------------------ 维修员 ------------------------------------------*/

  // 维修员首页
  static String repairHome = "/projectManager/repairHome";

  static void configureRouters(FluroRouter router) {
    /// 主入口
    router.define(indexPage, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      String type = params['type'].first;
      return IndexPage(type: type);
    }));

    /// 登录
    router.define(loginPage, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return LoginPage();
    }));

    /// 消息列表
    router.define(messageList, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return MessageList();
    }));

    /// 启动页
    router.define(splashPage, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return SplashPage();
    }));

    /// 报修记录列表
    router.define(repairList, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return RepairList();
    }));

    /// 报修记录详情
    router.define(repairDetail, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      return RepairDetail(id: id);
    }));

    /// 设备信息
    router.define(deviceInfo, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      int btnType = int.parse(params['btnType'].first);
      return DeviceInfo(id: id, btnType: btnType);
    }));

    /// 设备报修
    router.define(equipmentRepair, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      return EquipmentRepair(id: id);
    }));

    /* ---------------------------------保洁 ------------------------------------------ */

    router.define(cleanHome, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return CleanHome();
    }));
    router.define(cleanFeedback, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return CleanFeedback();
    }));

    router.define(feedbackDetail, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      return FeedbackDetail(id: id);
    }));

    router.define(materielApplyList, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return MaterielApplyList();
    }));

    router.define(onlineInspectionAdd, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int outletsId = int.parse(params['outletsId'].first);
      String outletsStr = params['outletsStr'].first;
      return OnlineInspectionAdd(outletsId: outletsId, outletsStr: outletsStr);
    }));

    router.define(materielApply, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return MaterielApply();
    }));

    router.define(materielSelect, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return MaterielSelect();
    }));

    router.define(materielDetail, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      return MaterielDetail(id: id);
    }));

    router.define(leaveApplyList, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return LeaveApplyList();
    }));

    router.define(leaveApply, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return LeaveApply();
    }));

    router.define(workArrange, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return WorkArrange();
    }));

    router.define(eventList, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return EventList();
    }));

    router.define(eventReport, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      String outStr = params['outStr'].first;
      return EventReport(outStr: outStr);
    }));

    router.define(cleanWorkRecord, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return CleanWorkRecord();
    }));

    router.define(eventDetail, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      return EventDetail(id: id);
    }));

    router.define(leaveApplyDetail, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      return LeaveApplyDetail(id: id);
    }));

    router.define(goToScheduling, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      return GoToScheduling(id: id);
    }));

    router.define(onlineInspectionRecord, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      return OnlineInspectionRecord(id: id);
    }));

    router.define(onlineInspectionDetail, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      return OnlineInspectionDetail(id: id);
    }));

    router.define(schedulingDemandList, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return SchedulingDemandList();
    }));

    /* -------------------------------- 领班 ------------------------------------------ */

    router.define(checkHome, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return CheckHome();
    }));

    router.define(checkMachineList, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return CheckMachineList();
    }));

    router.define(checkMachineInspect, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return CheckMachineInspect();
    }));

    router.define(checkMachineDetail, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      return CheckMachineDetail(id: id);
    }));

    router.define(checkCleanDemand, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return CheckCleanDemand();
    }));

    router.define(checkScheduling, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return CheckScheduling();
    }));

    router.define(cleanSelect, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      int type = int.parse(params['type'].first);
      int feedbackId = int.parse(params['feedbackId'].first);
      return CleanSelect(id: id, type: type, feedbackId: feedbackId);
    }));

    router.define(checkEmployeeList, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return CheckEmployeeList();
    }));

    router.define(onlineInspectionList, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return OnlineInspectionList();
    }));

    router.define(checkFeedbackList, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return CheckFeedbackList();
    }));

    router.define(checkEventReport, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      return CheckEventReport(id: id);
    }));

    router.define(outletsSelect, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int type = int.parse(params['type'].first);
      return OutletsSelect(type: type);
    }));

    router.define(floorSelect, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      int orgId = int.parse(params['orgId'].first);
      return FloorSelect(id: id, orgId: orgId);
    }));

    router.define(cleanerWork, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return CleanerWork();
    }));

    router.define(checkEventReportList, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return CheckEventReportList();
    }));

    router.define(checkMaterielExamine, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      return CheckMaterielExamine(id: id);
    }));

    router.define(checkComputationList, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int index = int.parse(params['index'].first);
      return CheckComputationList(index: index);
    }));

    router.define(employeeEntry, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      int isAdd = int.parse(params['isAdd'].first);
      return EmployeeEntry(id: id, isAdd: isAdd);
    }));

    router.define(occupationSelect, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      return OccupationSelect(id: id);
    }));

    router.define(dutyPublicSelect, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      return DutyPublicSelect(id: id);
    }));

    router.define(leaveApprovalList, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return LeaveApprovalList();
    }));

    router.define(checkWorkAttendance, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return CheckWorkAttendance();
    }));

    router.define(checkAddScheduling, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      String date = params['date'].first;
      int outId = int.parse(params['outId'].first);
      String outStr = params['outStr'].first;
      return CheckAddScheduling(
        date: date,
        outId: outId,
        outStr: outStr,
      );
    }));

    router.define(checkCleanMonthlyAssessment, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return CheckCleanMonthlyAssessment();
    }));

    router.define(materielExamineDetail, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      return MaterielExamineDetail(id: id);
    }));

    router.define(materielComputationDetail, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      return MaterielComputationDetail(id: id);
    }));

    router.define(checkEventDetail, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      return CheckEventDetail(id: id);
    }));

    router.define(checkLeaveApplyDetail, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      return CheckLeaveApplyDetail(id: id);
    }));

    router.define(examineNoticeDetail, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      return ExamineNoticeDetail(id: id);
    }));

    router.define(checkNoticeDetail, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      return CheckNoticeDetail(id: id);
    }));

    router.define(checkFeedbackDetail, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      return CheckFeedbackDetail(id: id);
    }));

    router.define(checkSchedulingEdit, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      String dateStr = params['dateStr'].first;
      return CheckSchedulingEdit(id: id, dateStr: dateStr);
    }));

    router.define(checkAssessmentScoring, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      String dateStr = params['dateStr'].first;
      return CheckAssessmentScoring(id: id, dateStr: dateStr);
    }));

    router.define(checkAssessmentScored, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      String dateStr = params['dateStr'].first;
      return CheckAssessmentScored(id: id, dateStr: dateStr);
    }));

    router.define(checkAssessmentDetail, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      String dateStr = params['dateStr'].first;
      return CheckAssessmentDetail(id: id, dateStr: dateStr);
    }));

    /* -------------------------------- 主管 ------------------------------------------ */
    router.define(regionHome, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return RegionHome();
    }));

    router.define(inventoryRecordList, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return InventoryRecordList();
    }));

    router.define(inventoryDetail, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      return InventoryDetail(id: id);
    }));

    router.define(assetInventory, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return AssetInventory();
    }));

    router.define(regionMaterialArea, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return RegionMaterialArea();
    }));

    /* -------------------------------- 银行 ------------------------------------------ */

    router.define(bankHome, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return BankHome();
    }));

    router.define(bankEvaluation, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return BankEvaluation();
    }));

    router.define(backCleanDemand, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int outId = int.parse(params['outId'].first);
      String outStr = params['outStr'].first;
      return BackCleanDemand(outId: outId, outStr: outStr);
    }));

    router.define(backSchedulingApply, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int outId = int.parse(params['outId'].first);
      String outStr = params['outStr'].first;
      return BackSchedulingApply(outId: outId, outStr: outStr);
    }));

    router.define(bankOutletsSelect, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int type = int.parse(params['type'].first);
      return BankOutletsSelect(type: type);
    }));

    router.define(bankFeedback, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return BankFeedback();
    }));

    router.define(backAppraiseDetail, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int type = int.parse(params['type'].first);
      int id = int.parse(params['id'].first);
      return BackAppraiseDetail(type: type, id: id);
    }));

    router.define(bankSchedulingDemandList, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return BankSchedulingDemandList();
    }));

    /* ------------------------------ 区域经理 ------------------------------------------*/
    router.define(projectHome, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return ProjectHome();
    }));

    router.define(projectOutletsDistribute, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return ProjectOutletsDistribute();
    }));

    router.define(projectCheckSelect, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int type = int.parse(params['type'].first);
      int id = int.parse(params['id'].first);
      int checkId = int.parse(params['checkId'].first);
      return ProjectCheckSelect(id: id, type: type, checkId: checkId);
    }));

    router.define(projectOutletsManageList, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return ProjectOutletsManageList();
    }));

    router.define(projectMaterielManageList, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return ProjectMaterielManageList();
    }));

    router.define(projectPersonnelAssessmentList, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return ProjectPersonnelAssessmentList();
    }));

    router.define(projectOutletsCheckList, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return ProjectOutletsCheckList();
    }));

    router.define(projectCustomerOpinionList, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return ProjectCustomerOpinionList();
    }));

    router.define(projectOutletsDetail, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      return ProjectOutletsDetail(id: id);
    }));

    router.define(projectOutletsManageDetail, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      return ProjectOutletsManageDetail(id: id);
    }));

    router.define(projectAppraiseDetail, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      String dateStr = params['dateStr'].first;
      return ProjectAppraiseDetail(id: id, dateStr: dateStr);
    }));

    router.define(projectMaterielManageDetail, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      return ProjectMaterielManageDetail(id: id);
    }));

    router.define(projectCheckAssessmentDetail, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      String dateStr = params['dateStr'].first;
      return ProjectCheckAssessmentDetail(id: id, dateStr: dateStr);
    }));

    router.define(projectMaterielComputationDetail, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      return ProjectMaterielComputationDetail(id: id);
    }));

    router.define(projectCustomerOpinionDetail, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      return ProjectCustomerOpinionDetail(id: id);
    }));

    router.define(messageDetail, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      int type = int.parse(params['type'].first);
      String title = params['title'].first;
      return MessageDetail(id: id, type: type, title: title);
    }));

    router.define(equipmentScrapList, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return EquipmentScrapList();
    }));

    router.define(equipmentScrapDetail, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      return EquipmentScrapDetail(id: id);
    }));

    router.define(reportRepair, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      int id = int.parse(params['id'].first);
      int submitType = int.parse(params['submitType'].first);
      return ReportRepair(id: id, submitType: submitType);
    }));

    /* ------------------------------ 维修员 ------------------------------------------*/

    // 维修员首页
    router.define(repairHome, handler: Handler(
        handlerFunc: (BuildContext context, Map<String, List<String>> params) {
      return RepairHome();
    }));
  }
}
